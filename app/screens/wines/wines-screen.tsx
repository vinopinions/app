import { Text, View } from 'react-native';

const WinesScreen = () => {
    return (
        <View>
            <Text>This is the the wine screen</Text>
        </View>
    );
};

export default WinesScreen;
